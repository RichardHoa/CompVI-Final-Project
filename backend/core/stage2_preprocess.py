def preprocess_image(raw_frame):
    """
    Stage 2: Image Preprocessing
    Applies operations like resizing, CLAHE for lighting normalization, 
    and Gaussian Blur for noise reduction.
    """
    print("[Stage 2] Preprocessing image (Resize -> CLAHE -> Gaussian Blur)")
    
    # Mocking the preprocessing
    # frame_resized = cv2.resize(raw_frame, (640, 480))
    # ... apply CLAHE and Blur ...
    processed_frame = "MOCK_PROCESSED_FRAME"
    
    return processed_frame
