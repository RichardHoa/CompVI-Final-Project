def detect_face(processed_frame):
    """
    Stage 3: Face Detection
    Uses models like MTCNN, RetinaFace, or HaarCascade to locate a face.
    Returns the bounding box coordinates (ROI).
    """
    print("[Stage 3] Detecting face in frame")
    
    # Mocking face detection bounding box [x, y, w, h]
    mock_roi = [100, 100, 200, 200]
    
    return mock_roi
